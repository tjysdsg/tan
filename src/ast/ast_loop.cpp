#include "src/ast/ast_loop.h"

namespace tanlang {

llvm::Value *ASTLoop::codegen(tanlang::CompilerSession *compiler_session) {
  throw std::runtime_error("FUCK");
  // return ASTNode::codegen(compiler_session);
}

} // namespace tanlang


