fn assert(a: u32) : void;

fn main(argc: int, argv: char**) : int {
    var a: i32 = 0;
    var b: i32 = 1;
    var shit: i32 = !(a == b);
    // var shit: i32 = !(1 == 0); // TODO: implicit type conversion
    assert(shit);
    return 0;
}
