import "runtime/debug.tan";
import "runtime/print.tan";

pub fn main() : int {
    var n = 1;
    var pn = &n;
    assert(n == 1);
    // TODO: assert(pn.* == 1);
    // TODO: pn.* = 2;
    // assert(n == 2);
    // TODO: assert(pn.* == 2);
    print("SUCCESS\n");
    return 0;
}
