extern fn print(s: str) : int;

pub fn main(argc: int, argv: char**) : int {
    var a: i32 = 0;
    var b: i32 = 1;
    var shit: i32 = !(a == b);
    @assert(shit);
    @assert(1);
    @assert(@file);
    @assert(@line);
    var argc_decl: str = @get_decl(argc);
    print("SUCCESS\n");
    return 0;
}
