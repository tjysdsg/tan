import "runtime/print.tan";
import "runtime/debug.tan";
import "runtime/math.tan";

enum E {
    e1,
    e2,
    e3,
}

pub fn main(argc: int, argv: char**) : int {
    var enum1 = E.e1;
    // var enum2: E = 1; // this should raise compile error
    var enum2: E = 1 as E; // but this should work
    print("SUCCESS");
    return 0;
}
