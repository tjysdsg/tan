struct StackTrace {
  var filename: str;
  var src: str;
  var lineno: u32;
};

fn assert(a: u32) : void {
    if (!a) {
        var st: StackTrace = @stack_trace(1);
        __assert(st.src, st.filename, st.lineno);
    }
}
